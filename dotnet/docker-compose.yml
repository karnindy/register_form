services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8085:8085"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8085
      - TZ=Asia/Bangkok
      - ConnectionStrings__DefaultConnection=Server=host.docker.internal;Database=thaiairp_iptc;User Id=sa;Password=Pass@456981@XKTT;TrustServerCertificate=True;
      - ConnectionStrings__MySqlConnection=Server=host.docker.internal;Database=thaiairp_iptc;User=thaiairp_iptc;Password=Pass@456981@XKTT;SslMode=none;AllowPublicKeyRetrieval=True;
    volumes:
      - ./backend/LocalData:/App/LocalData
    # Uncomment and configure depends_on if you add a mysql service locally
    # depends_on:
    #   - mysql

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:80"
    depends_on:
      - backend
